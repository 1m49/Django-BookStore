{
    "builds": [{
        "src": "BookStore/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "BookStore/wsgi.py"
        }
    ]
}